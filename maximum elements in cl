static int maximum(CNode head) {

    if (head == null)
        return Integer.MIN_VALUE;

    int max = head.data;

    CNode temp = head.next;

    while (temp != head) {

        max = Math.max(max, temp.data);

        temp = temp.next;
    }

    return max;
}
